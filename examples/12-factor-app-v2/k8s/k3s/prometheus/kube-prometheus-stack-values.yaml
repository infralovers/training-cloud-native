prometheus:
  prometheusSpec:
    additionalScrapeConfigs:
      - job_name: '12-factor-app/api'
        scrape_interval: 5s
        static_configs:
        - targets: [api.12-factor-app.svc.cluster.local:3030]
          labels:
            app: api
            port: 3030
    retention: 3d