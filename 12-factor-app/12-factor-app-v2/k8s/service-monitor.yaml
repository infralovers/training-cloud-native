kind: ServiceMonitor
metadata:
  name: api
spec:
  selector:
    matchLabels:
      app: api
  namespaceSelector:
    name: 12-factor-app      
  endpoints:
  - port: metrics # Here we use the name of the port defined earlier
